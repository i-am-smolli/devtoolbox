# Name of your application. Used to uniquely configure containers.
service: devtoolbox
image: iamsmolli87/iamsmolli

registry:
  username: iamsmolli87
  password:
    - KAMAL_REGISTRY_PASSWORD

builder:
  arch: amd64

# Deploy to these servers.
servers:
  - ubuntu@dev.jonathanmiller.icu

proxy:
  ssl: true
  host: dev.jonathanmiller.icu
  app_port: 3000

